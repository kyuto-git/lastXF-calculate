ターミナル（またはコマンドプロンプト）を開き、docker-lasthalon-calculateディレクトリに移動します。次に、以下のコマンドを実行してDockerイメージをビルドします：

docker build -t csv_processor .

コードの修正を行った際にはその都度、上記でリビルドし直す。
# CSVファイルの読み込み（Shift-JISエンコーディング）
の読み込みファイルは、ローカルに置いてるファイル名が変わったらその都度変える

docker run --rm -v $(pwd):/app csv_processor


--------------------


. コンテナのログの確認
コンテナのログを確認して、スクリプトの実行中にエラーやその他のメッセージが出力されていないかを確認します。コンテナのIDや名前が必要です。これは docker ps -a コマンドで一覧表示されます。


docker logs [コンテナIDまたはコンテナ名]

このコマンドで表示されるログにエラーメッセージやダウンロードの進捗情報などが表示されます。